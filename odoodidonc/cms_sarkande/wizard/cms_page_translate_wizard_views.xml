<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Translation Line List View -->
    <record id="view_cms_page_translate_line_list" model="ir.ui.view">
        <field name="name">cms.page.translate.line.list</field>
        <field name="model">cms.page.translate.line</field>
        <field name="arch" type="xml">
            <list editable="bottom" create="0" delete="0">
                <field name="block_name" readonly="1"/>
                <field name="field_label" readonly="1"/>
                <field name="source_value" readonly="1" widget="html"/>
                <field name="translated_value" widget="html"/>
                <!-- Hidden fields -->
                <field name="block_id" column_invisible="1"/>
                <field name="component_model" column_invisible="1"/>
                <field name="component_id" column_invisible="1"/>
                <field name="component_field" column_invisible="1"/>
                <field name="is_html_content" column_invisible="1"/>
                <field name="wizard_id" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Wizard Form View -->
    <record id="view_cms_page_translate_wizard_form" model="ir.ui.view">
        <field name="name">cms.page.translate.wizard.form</field>
        <field name="model">cms.page.translate.wizard</field>
        <field name="arch" type="xml">
            <form string="Traduire la page">
                <sheet>
                    <group>
                        <field name="page_id" invisible="1"/>
                        <field name="lang_id" options="{'no_create': True, 'no_open': True}"/>
                    </group>

                    <group string="Traductions">
                        <field name="translation_line_ids" nolabel="1" colspan="2"/>
                    </group>
                </sheet>
                <footer>
                    <button string="Sauvegarder" type="object" name="action_save_translations" class="btn-primary"/>
                    <button string="Annuler" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
